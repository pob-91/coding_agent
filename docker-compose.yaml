services:
  main-db:
    image: couchdb:3
    restart: always
    environment:
      - COUCHDB_USER=admin
      - COUCHDB_PASSWORD=password
    ports:
      - "5984:5984"
    volumes:
      - couchdb_data:/opt/couchdb/data
    networks:
      - internal-network

volumes:
  couchdb_data:

networks:
  internal-network:
    name: internal-network
