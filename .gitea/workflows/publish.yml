name: Build and Push Coding Agent Docker Image

on:
  push:
    tags:
      - "*"

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to gitea registry
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | docker login "${{ vars.REPO_URL }}" -u "${{ gitea.actor }}" --tls-verify=false --password-stdin

      - name: Set up Docker Buildx
        run: docker buildx create --use

      - name: Build and push
        run: |
          docker buildx build \
            --push --tls-verify=false \
            --tag ${{ vars.REPO_URL }}/${{ gitea.repository_owner }}/coding-agent:${{ gitea.ref_name }} \
            .
