[project]
name = "coding-agent"
version = "0.1.0"
description = "Agent that takes tasks from an webhook and raises MRs."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.129.0",
    "pydantic>=2.12.5",
    "python-dotenv>=1.2.1",
    "uvicorn>=0.40.0",
]
